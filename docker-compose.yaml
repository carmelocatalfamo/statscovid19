version: "3.4"

services:
  statscovid19:
    build:
      context: .
      dockerfile: docker/dev.Dockerfile
    container_name: statscovid19
    env_file: .env
    ports:
      - 3000:3000
    tty: true
    volumes:
      - ./:/app
      - /app/node_modules/
